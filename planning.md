# Basic Authentication Flow — Planning

This file signals the downstream workflow to create agent issues.
The full implementation plan is located at [`planned/plan.md`](planned/plan.md).

## Summary

Build a simple authentication application using FastAPI (backend) and FastUI (frontend).

- **UI/UX**: Login screen with username and password fields (`planned/plan.md` § Frontend — FastUI)
- **Backend**: FastAPI REST endpoint that validates credentials against hardcoded values (`planned/plan.md` § Backend — FastAPI)
- **Testing**: Unit and integration tests with pytest (`planned/plan.md` § Testing)
