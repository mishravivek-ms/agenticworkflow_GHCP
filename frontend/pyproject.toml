[project]
name = "agenticworkflow-frontend"
version = "0.1.0"
description = "FastUI frontend for authentication flow"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "fastui",
    "httpx>=0.27.0",
    "uvicorn>=0.27.0",
]

[tool.uv]
package = false
