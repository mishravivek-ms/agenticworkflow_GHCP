[project]
name = "auth-frontend"
version = "0.1.0"
description = "FastUI frontend for the authentication flow"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110.0",
  "fastui[fastapi]>=0.4.0",
  "httpx>=0.27.0",
  "pydantic>=2.6.0",
  "uvicorn>=0.27.0",
]
