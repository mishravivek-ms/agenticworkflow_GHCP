[project]
name = "auth-backend"
version = "0.1.0"
description = "FastAPI backend for the authentication flow"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110.0",
  "pydantic>=2.6.0",
  "uvicorn>=0.27.0",
]
